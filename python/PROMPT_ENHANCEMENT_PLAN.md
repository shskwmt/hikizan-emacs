# Task List: Agent Prompt Enhancement

This document outlines the tasks for analyzing and enhancing the system prompts for the Emacs Agent and its sub-agents to improve robustness, precision, and alignment with the "Hikizan" (minimalist) philosophy.

## Completed Tasks
- [x] **Analysis**: Evaluated `AGENTS.md`, `common_prompts.py`, and individual agent definitions in `python/emacs_agent/sub_agents/`.
- [x] **Common Instructions**: Updated `common_prompts.py` with robust Elisp usage guidelines, including:
    - Mandatory surgical edits using `search-forward` and `replace-match`.
    - Use of `atomic-change-group` for consistent modifications.
    - Explicit `(save-buffer)` calls to ensure persistence.
    - Background file handling with `find-file-noselect`.
- [x] **Orchestrator Enhancement**: Updated `emacs_agent` in `python/emacs_agent/agent.py` to emphasize:
    - **Chain of Thought**: Reasoning before delegation.
    - **Verification Loop**: Mandatory testing/review after implementation.
    - **Iterative Refinement**: Better handling of sub-agent failures.
- [x] **Sub-Agent Specialization**: Updated all 12 sub-agents with specialized roles:
    - **Task Planner**: Added Dependency Mapping and Risk Analysis.
    - **Git Operator**: Reinforced Conventional Commits and safety confirmations.
    - **Debugger**: Focused on `*Messages*`, `*Warnings*`, and `edebug`.
    - **Refactor**: Deeply integrated the "Hikizan" philosophy (subtraction/simplification).
- [x] **Bug Fixes**: Corrected string escaping (double-escaping backslashes) and formatting issues in the updated Python-embedded prompts.

## Pending Tasks
- [x] **Plan Documentation**: Output this task list as a Markdown file (Completed by this action).
- [ ] **System Verification**: Execute a complex multi-agent task to verify the new prompts in a real-world scenario.
- [ ] **Self-Reflection**: Run the `self_reflection` agent to evaluate the impact of these changes on workflow efficiency.

---
*Generated by TASK PLANNER*
