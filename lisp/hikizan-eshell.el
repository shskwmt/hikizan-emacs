;;; hikizan-eshell.el --- eshell  -*- lexical-binding: t; -*-

;;; Configurations

(setq eshell-history-size 10000
      eshell-save-history-on-exit t
      eshell-hist-ignoredups t
      eshell-history-append t)

(provide 'hikizan-eshell)
